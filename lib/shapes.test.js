//Import constructor Shapes
const shapeMaker = require('./shapes.js');

//Create test suite for Shapes
describe('Shapes', () => {
    //A test for Circle is created
    describe('circle', () => {
        it()
    });
    //A test for Square is created
    describe('square', () => {
        it()
    });
    //A test for Triangle is created
    describe('triangle', () => {
        it()
    });
});

// include a render() method above that returns a string for the corresponding svg and shape color

This test should pass
//const shape = new Triangle();
// shape.setColor("blue");
// expect(shape.render()).toEqual('<polygon points="150, 18 244, 182 56, 182" fill="blue" />');

//add additional files to the lib folder for user input, writing to a file, etc.